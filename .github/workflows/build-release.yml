name: Build and Release Executable

on:
  push:
    branches: [ main ]

permissions:
  contents: write  

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install poetry
        poetry install
        pip install -r requirements.txt
        pip install pyinstaller
        
    - name: Build executable
      run: |
        pyinstaller --onefile --name PhotoDisarm --hidden-import tkinter --hidden-import cv2 --hidden-import numpy --hidden-import rawpy --hidden-import PIL --hidden-import PIL.Image --hidden-import PIL.ImageFont --hidden-import PIL.ImageDraw --hidden-import PIL._tkinter --paths ./src --collect-submodules photodisarm src/main.py
        
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: release-${{ github.run_number }}
        release_name: Release ${{ github.run_number }}
        draft: false
        prerelease: false
        
    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./dist/PhotoDisarm.exe
        asset_name: PhotoDisarm.exe
        asset_content_type: application/octet-stream